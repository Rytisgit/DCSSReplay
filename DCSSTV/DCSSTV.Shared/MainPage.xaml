<Page
    x:Class="DCSSTV.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DCSSTV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:SkiaSharp.Views.UWP"
    xmlns:not_win="http://uno.ui/not_win"
    mc:Ignorable="d not_win">

      <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <CheckBox Content="Hardware Accelerated" x:Name="hwAcceleration" />
        <Grid Grid.Row="1" Width="1602" Height="768" VerticalAlignment="Top">
            <views:SKSwapChainPanel
                x:Name="swapChain"
                PaintSurface="OnPaintSwapChain"
                Visibility="{x:Bind hwAcceleration.IsChecked, Mode=OneWay}" />
            <views:SKXamlCanvas 
                x:Name="canvas" 
                PaintSurface="OnPaintSurface"
                Visibility="{x:Bind Not(hwAcceleration.IsChecked), Mode=OneWay}" />
        </Grid>
        <StackPanel>
            <RelativePanel>


                <Button Content="Start playback" Click="Button_Click3" Margin="380,0,0,0"/>
                <TextBox Text="2" x:Name="speed" Margin="490,0,0,0" />
                <TextBox Text="10" x:Name="framerate" Margin="560,0,0,0" />
                <Button Content="First Time img cache setup" Click="LoadPackageFile" Margin="175,0,0,0"/>
                <Button Content="Stop Playback" Click="Button_Click2" Margin="700,0,0,0"/>

            </RelativePanel>

        </StackPanel>
        <TextBlock Grid.Row="1" x:Name="output"/>
    </Grid>
</Page>
