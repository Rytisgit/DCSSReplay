<Page
    x:Class="DCSSTV.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DCSSTV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:SkiaSharp.Views.UWP"
    xmlns:not_win="http://uno.ui/not_win"
    mc:Ignorable="d not_win">

      <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <CheckBox Content="Hardware Accelerated" x:Name="hwAcceleration" />
        <Grid Grid.Row="1" Width="1602" Height="768" VerticalAlignment="Top">
            <views:SKSwapChainPanel
                x:Name="swapChain"
                PaintSurface="OnPaintSwapChain"
                Visibility="{x:Bind hwAcceleration.IsChecked, Mode=OneWay}" />
            <views:SKXamlCanvas 
                x:Name="canvas" 
                PaintSurface="OnPaintSurface"
                Visibility="{x:Bind Not(hwAcceleration.IsChecked), Mode=OneWay}" />
        </Grid>
        <StackPanel>
            <TextBox Header="Package asset file name" Text="ms-appx:///Assets/MyPackageAsset.txt" x:Name="assetFileName" Width="1582" />
            <RelativePanel>

                 <Button Content="Do It" Click="Button_Click"/>
                <Button Content="filePick" Click="OnLogoButtonClicked" Margin="100,0,0,0"/>
                <Button Content="loop with delay" Click="Button_Click3" Margin="230,0,0,0"/>
                <TextBox Text="100" x:Name="speed" Margin="350,0,0,0" />
                <Button Content="load Zip" Click="LoadPackageFile" Margin="500,0,0,0"/>
                <Button Content="end" Click="Button_Click2" Margin="170,0,0,0"/>

            </RelativePanel>

        </StackPanel>
        <TextBlock Grid.Row="1" x:Name="output"/>
    </Grid>
</Page>
